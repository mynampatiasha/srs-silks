MONGO_URI=mongodb://<user>:<password>@<cluster-host>/<db-name>?retryWrites=true&w=majority
PORT=5000
JWT_SECRET=<random-long-string>

# Used only once by scripts/seedOwner.js to create the first admin login.
OWNER_NAME=Owner
OWNER_EMAIL=<owner-email>
OWNER_PASSWORD=<strong-password>
